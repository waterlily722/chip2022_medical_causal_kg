# CHIP2022 医疗因果事件知识图谱：构建、推理与 GraphRAG 增强问答系统

> 知识图谱课程实验报告

---

## 目录

1. [问题背景与任务定义](#1-问题背景与任务定义)
2. [数据与知识图谱构建](#2-数据与知识图谱构建)
3. [推理方法设计](#3-推理方法设计)
4. [大模型增强方法](#4-大模型增强方法)
5. [实验与结果分析](#5-实验与结果分析)
6. [可视化系统](#6-可视化系统)
7. [小组分工](#7-小组分工)
8. [总结与不足](#8-总结与不足)

---

## 1. 问题背景与任务定义

### 1.1 领域背景

医学领域存在大量因果关系知识，例如"高血压导致主动脉粥样硬化""卵巢功能衰退导致月经量减少"等。这些因果链条往往跨越多个环节，形成复杂的因果网络。传统的文本检索难以直接回答"为什么高血压可能和心肌梗死有关"这类多跳因果推理问题。

知识图谱（Knowledge Graph）能够将碎片化的因果关系结构化为三元组，并通过图推理发现隐含的多跳因果路径。然而，单纯依赖知识图谱推理的回答形式较为僵硬，缺乏自然语言理解能力；而大语言模型（LLM）虽然具备强大的生成能力，但存在幻觉（hallucination）问题，可能编造不存在的医学因果关系。

### 1.2 任务目标

本项目旨在构建一个**医疗因果事件知识图谱**，并实现基于**GraphRAG（图谱增强检索生成）** 的问答系统。具体目标包括：

1. **知识图谱构建**：从 CHIP2022 医疗因果实体关系抽取数据集中，通过人工标注种子 + Qwen LLM 抽取扩展的混合方式，构建大规模医疗因果事件知识图谱。
2. **知识推理**：实现多种推理方式（直接/反向因果推理、多跳路径推理、条件约束推理、上下位推理），支持因果链发现。
3. **大模型增强问答**：利用 GraphRAG 将知识图谱证据注入大模型提示词，对比 LLM-only、KG-only、KG-enhanced 三种模式在准确性、证据可追溯性和幻觉减少方面的表现。

### 1.3 为什么该领域适合构建知识图谱

- **因果关系天然具有图结构**：医学因果关系形成有向图（原因 → 结果），适合用三元组表示。
- **多跳推理需求**：临床诊断和病理分析常需要沿因果链条回溯，图推理天然支持路径发现。
- **条件约束复杂**：许多因果关系具有前置条件（如"在人工流产术三个月后发现宫腔粘连"），需要专门的 `condition_of` 关系建模。
- **大模型在该任务中的不足**：通用 LLM 可能输出不准确或编造的医学因果知识，知识图谱可以提供可验证的证据链来约束模型输出。

---

## 2. 数据与知识图谱构建

### 2.1 数据来源

本项目使用 **CHIP2022（中国健康信息处理大会）** 医疗因果实体关系抽取数据集，该数据集为公开标准数据集，包含中文医学文本及人工标注的因果关系。

| 数据文件 | 用途 | 文档数 | 说明 |
|---|---|---|---|
| `train_0717.json` | 构建 Gold Seed KG | 999 | 包含人工标注的 `relation_of_mention`（关系类型 1/2/3） |
| `unlabel.json` | Qwen 抽取扩展 | 1,000 | 仅含原始文本，无标注，使用 Qwen API 抽取关系 |
| `testA.json` | 测试集 | — | 用于 Qwen 抽取扩展 |
| `testB.json` | 测试集 | — | 用于 Qwen 抽取扩展 |

**数据格式示例**（`train_0717.json` 中一条文档）：

```json
{
  "text": "40岁是女人卵巢功能开始衰退的时间段,如果月经量开始慢慢减少有可能是卵巢功能衰退...",
  "relation_of_mention": [
    {
      "head": {"mention": "宫腔操作后导致内膜变薄、粘连", "start_idx": 61, "end_idx": 75},
      "relation": 1,
      "tail": {"type": "mention", "mention": "月经量少", "start_idx": 77, "end_idx": 81}
    },
    {
      "head": {"mention": "女性激素非常好", "start_idx": 97, "end_idx": 104},
      "relation": 2,
      "tail": {
        "type": "relation",
        "head": {"mention": "宫腔粘连", "start_idx": 124, "end_idx": 128},
        "relation": 1,
        "tail": {"mention": "月经量少", "start_idx": 116, "end_idx": 120}
      }
    }
  ]
}
```

CHIP2022 标注三种关系类型：

| 原始标签 | 映射关系 | 说明 |
|---|---|---|
| 1 | `causes` | 因果关系：原因导致结果 |
| 2 | `condition_of` | 条件关系：条件修饰一条因果关系 |
| 3 | `is_a` | 上下位关系：下位概念属于上位概念 |

其中 `condition_of`（relation=2）的 tail 是一个嵌套关系，表示条件修饰的因果对。例如"女性激素非常好"作为条件修饰"宫腔粘连导致月经量少"这一因果关系。

### 2.2 Schema 设计

#### 2.2.1 实体类型

项目设计了 11 种实体类型，覆盖医学因果事件中的主要概念：

| 实体类型 | 说明 | 示例 |
|---|---|---|
| `Disease` | 疾病 | 高血压、心肌梗死、胃溃疡 |
| `Symptom` | 症状 | 月经量少、后脑头疼、手指发麻 |
| `ClinicalSign` | 临床体征 | 脑干功能受限 |
| `PathologicalState` | 病理状态 | 宫腔粘连、动脉粥样硬化 |
| `RiskFactor` | 危险因素 | 吸烟、饮酒、高血脂 |
| `TestResult` | 检查结果 | 女性激素非常好 |
| `ExamProcedure` | 检查手段 | B超检查、宫腔镜检查 |
| `Treatment` | 治疗方式 | 人工流产、刮宫手术 |
| `AnatomicalSite` | 解剖部位 | 主动脉、胃部 |
| `MedicalCategory` | 医学分类 | 消化系统器质性病变 |
| `Other` | 其他 | 无法归类的医学实体 |

#### 2.2.2 关系类型

| 关系类型 | 方向 | 说明 |
|---|---|---|
| `causes` | 原因 → 结果 | 明确因果关系（X 导致 Y） |
| `risk_factor_for` | 危险因素 → 疾病 | 危险因素增加发病风险 |
| `condition_of` | 条件 → 原因 | 条件修饰一条因果关系 |
| `is_a` | 下位 → 上位 | 上下位概念关系 |
| `symptom_of` | 症状 → 疾病 | 症状属于某疾病 |
| `treated_by` | 疾病 → 治疗 | 疾病的治疗方式 |
| `located_in` | 疾病 → 部位 | 疾病所在的解剖位置 |
| `diagnosed_by` | 疾病 → 检查 | 疾病的诊断方法 |

**`condition_of` 的特殊建模**：

`condition_of` 关系将条件实体连接到被修饰的因果关系中的原因实体，同时保留原始的因果边。例如：

```
输入文本："如果女性激素非常好说明还没有到围绝经状态，月经量少有可能是宫腔粘连造成的"

转换为：
  宫腔粘连 --causes--> 月经量少           （因果边）
  女性激素非常好 --condition_of--> 宫腔粘连  （条件边，指向原因）
```

此外，对于包含完整条件-原因-结果的 `condition_of` 三元组，系统额外生成 `CausalEvent` 节点，记录完整的因果事件结构。

### 2.3 信息抽取方法

#### 2.3.1 Gold Seed KG 构建（来自 train_0717.json）

从人工标注数据直接构建高质量种子图谱：

1. **解析 CHIP2022 JSON 格式**：读取 `relation_of_mention` 字段，提取 head/tail mention 及关系类型。
2. **关系映射**：将原始标签 1/2/3 映射为 `causes`/`condition_of`/`is_a`。
3. **condition_of 嵌套关系处理**：当 relation=2 时，tail 是嵌套关系（包含 cause-effect 对），系统同时提取条件三元组和因果三元组。
4. **实体类型推断**：由于 CHIP2022 训练标签不包含实体类别，使用基于规则的医学实体分类器推断类型。分类器基于关键词匹配和上下文特征（如"病""炎""症"→ Disease，"疼痛""减少""发麻"→ Symptom 等）。
5. **三元组去重**：基于 (head, relation, tail) 三元组去重，保留优先级为 gold > qwen > inferred。

#### 2.3.2 Qwen LLM 抽取（来自 unlabel.json）

对 1000 篇无标注医学文本，使用 Qwen API 进行关系抽取。抽取 Prompt 限定了 8 种关系类型和 11 种实体类型，要求每条关系附带原文证据和置信度，并提供少样本示例引导模型输出格式。

**抽取流程**：

```
unlabel.json 文本
    ↓ Prompt 封装
Qwen API 调用
    ↓ 解析与清洗
清洗后结果 → qwen_extracted_clean.json
    ↓ 与 Gold Seed KG 合并去重
最终图谱 → data/triples.csv
```

#### 2.3.3 抽取结果示例

Qwen 抽取的三元组示例：

```json
{
  "head": "枕骨大孔疝",
  "head_type": "Disease",
  "relation": "is_a",
  "tail": "脑疝",
  "tail_type": "Disease",
  "evidence": "分为两种，一个是枕骨大孔疝",
  "source_doc_id": "Doc_unlabel_0000",
  "source_type": "qwen",
  "confidence": 0.95
}
```

### 2.4 图谱存储

使用 **CSV + JSON** 文件存储，兼顾可读性和程序处理便利性：

| 文件 | 说明 |
|---|---|
| `data/triples.csv` | 全部三元组（18,539 条），含 head/head_type/relation/tail/tail_type/evidence/source_doc_id/source_type/confidence |
| `data/processed/entities.csv` | 实体列表（14,596 个），含 entity/type/frequency |
| `data/processed/relations.csv` | 关系统计（7 种关系类型的频次） |
| `data/processed/causal_events.csv` | 因果事件表（783 个 CausalEvent） |
| `data/processed/kg.json` | 图谱 JSON（节点+边），用于可视化 |

推理模块 `src/reasoning.py` 使用 Python 字典构建内存索引（邻接表），支持高效的多跳查询。

### 2.5 图谱规模与质量

#### 2.5.1 总体规模

| 指标 | 数值 |
|---|---|
| 实体数量 | **14,596** |
| 三元组数量 | **18,539** |
| 实体类型数量 | **11** |
| 关系类型数量 | **7** |
| 因果事件数量 | **783** |

> 远超课程要求（实体 ≥100，三元组 ≥300，实体类型 ≥3，关系类型 ≥3）。

#### 2.5.2 关系分布

| 关系类型 | 数量 | 占比 |
|---|---|---|
| `causes` | 10,709 | 57.8% |
| `treated_by` | 2,283 | 12.3% |
| `symptom_of` | 1,970 | 10.6% |
| `is_a` | 1,852 | 10.0% |
| `condition_of` | 584 | 3.1% |
| `diagnosed_by` | 575 | 3.1% |
| `located_in` | 566 | 3.1% |

因果关系（`causes`）占据绝对主导地位（57.8%），符合医疗因果事件知识图谱的定位。`treated_by` 和 `symptom_of` 由 Qwen 抽取贡献，扩展了图谱的语义覆盖范围。

#### 2.5.3 来源分布

| 来源 | 三元组数量 | 占比 |
|---|---|---|
| Gold（人工标注） | 9,126 | 49.2% |
| Qwen（LLM 抽取） | 9,413 | 50.8% |

Gold Seed KG 和 Qwen-extracted KG 各占约一半，体现了"高质量种子 + 大规模扩展"的混合构建策略。

### 2.6 图谱样例

以"高血压 → 心肌梗死"因果链为例，图谱中的相关三元组：

```
高血压 --causes--> 主动脉粥样硬化          [Gold, Doc_train_0717_0002]
高血压 --causes--> 冠心病                  [Gold, Doc_train_0717_0209]
高血压 --causes--> 心绞痛                  [Gold, Doc_train_0717_0209]
高血压 --causes--> 心肌梗死                [Gold, Doc_train_0717_0209]
高血压 --causes--> 心肌负荷增加            [Gold, Doc_train_0717_0209]
主动脉粥样硬化 --causes--> 心肌梗死        [Gold, Doc_train_0717_0002]
主动脉粥样硬化 --causes--> 脑卒中          [Gold, Doc_train_0717_0002]
收缩压大于180mmhg --condition_of--> 高血压 [Gold, Doc_train_0717_0209]
```

---

## 3. 推理方法设计

### 3.1 推理任务

系统支持以下十类推理任务，覆盖全部 7 种关系类型：

| 推理类型 | 输入示例 | 涉及关系 |
|---|---|---|
| 直接因果推理 | "急性扁桃体炎可能导致什么？" | `causes` |
| 反向因果推理 | "影响发音可能由什么引起？" | `causes` |
| 多跳路径推理 | "为什么烟雾病可能和上位脑神经元受到压迫有关？" | `causes` |
| 条件约束推理 | "在什么条件下支气管哮喘可能发生？" | `condition_of` |
| 上下位推理 | "阴道出血属于什么类型？" | `is_a` |
| 症状查询 | "双硫仑反应有哪些症状？" | `symptom_of` |
| 治疗方式查询 | "血钙的异常如何治疗？" | `treated_by` |
| 部位查询 | "脑出血发生在什么部位？" | `located_in` |
| 诊断方式查询 | "胃食管反流如何诊断？" | `diagnosed_by` |
| 跨关系推理 | "肝癌晚期有哪些症状？可能由什么引起？" | `symptom_of` + `causes` |

### 3.2 推理算法

#### 3.2.1 图索引构建

系统在初始化时构建内存图索引，包括实体的出边/入边邻接表和因果邻接表，支持高效的单跳和多跳查询。

#### 3.2.2 实体匹配

使用**子串匹配**将自然语言问题中的实体映射到图谱节点：按匹配长度降序排列，优先匹配更长的实体名，并去除已被长实体包含的短实体。

#### 3.2.3 多跳路径推理

采用**广度优先搜索（BFS）** 发现因果路径（最多 3 跳），结合最短路径剪枝策略，有效发现实体间的间接因果关系。

#### 3.2.4 推理决策流程

`answer_query()` 方法按以下优先级依次尝试推理：

1. **多跳路径推理**：当问题中出现 ≥2 个实体，且包含"为什么""是否""间接""有关"等关键词时触发。
2. **条件推理**：当问题包含"条件""情况下"等关键词时，查找 `condition_of` 入边。
3. **上下位推理**：当问题包含"属于""类型""哪类"等关键词时，查找 `is_a` 边。
4. **症状推理**：当问题包含"症状""表现""体征"等关键词时，查找 `symptom_of` 入边。
5. **治疗推理**：当问题包含"治疗""如何治疗""用药"等关键词时，查找 `treated_by` 出边。
6. **部位推理**：当问题包含"部位""位置""发生在"等关键词时，查找 `located_in` 出边。
7. **诊断推理**：当问题包含"诊断""检查""确诊"等关键词时，查找 `diagnosed_by` 出边。
8. **反向因果推理**：当问题包含"由什么""什么引起""原因"等关键词时，查找因果入边。
9. **直接因果推理**：默认查询因果出边。

### 3.3 推理样例

**示例 1：多跳因果链推理**

```
输入：python src/reasoning.py --query "高血压是否可能间接导致心肌梗死？"

匹配实体：高血压、心肌梗死
推理结果：
  存在因果路径：高血压 --causes--> 主动脉粥样硬化 --causes--> 心肌梗死
  证据链：
  - 高血压 --causes--> 主动脉粥样硬化 | 高血压要终身服药...因为高血压是主动脉粥样硬化最主要的危险因素...
  - 主动脉粥样硬化 --causes--> 心肌梗死 | ...主动脉粥样硬化是引起心肌梗死以及脑卒中最主要的原因...
```

**示例 2：条件约束推理**

```
输入：python src/reasoning.py --query "在什么条件下宫腔粘连可能导致月经量少？"

匹配实体：宫腔粘连
推理结果：
  可能的条件包括：女性激素非常好、人工流产术三个月后
  证据链：
  - 女性激素非常好 --condition_of--> 宫腔粘连
  - 人工流产术三个月后 --condition_of--> 宫腔粘连
```

**示例 3：上下位推理**

```
输入：python src/reasoning.py --query "胃溃疡属于什么类型？"

匹配实体：胃溃疡
推理结果：
  胃溃疡 属于：消化系统器质性病变
```

### 3.4 正确与错误案例分析

**正确案例：多跳因果链推理**

```
问题：为什么烟雾病可能和上位脑神经元受到压迫有关？
匹配实体：烟雾病、上位脑神经元受到压迫
推理结果：
  存在因果路径：烟雾病 --causes--> 脑出血 --causes--> 上位脑神经元受到压迫
```

系统成功发现了"烟雾病 → 脑出血 → 上位脑神经元受到压迫"的两跳因果路径。

**正确案例：症状查询**

```
问题：双硫仑反应有哪些症状？
匹配实体：双硫仑反应
推理结果：
  "双硫仑反应"的症状包括：血压升高、心率升高、恶心、呕吐、呼吸抑制、面色潮红、胸闷、呼吸困难
```

**正确案例：诊断查询**

```
问题：肠癌如何诊断？
匹配实体：肠癌
推理结果：
  "肠癌"可通过以下方式诊断：纤维结直肠镜、肠镜、肿瘤学指标
```

**错误案例：实体匹配误匹配**

```
问题：黄癣有哪些症状？
匹配实体：['症状', '黄癣']  ← "症状"被错误匹配为实体
推理结果：知识图谱中没有足够证据。
```

"症状"二字恰好是某个三元组中的实体名，导致系统将"症状"作为第一个匹配实体进行查询，而非"黄癣"。这说明子串匹配策略存在缺陷，需要引入更精确的实体识别机制。

**错误案例：负面检查误触发**

```
问题：图谱中有没有证据说明大便像果冻会导致慢性咽喉炎反复急性发作？
匹配实体：['慢性咽喉炎反复急性发作', '大便像果冻']
推理结果：
  "慢性咽喉炎反复急性发作"可能导致：咽喉经常发炎
```

问题本意是检查是否存在因果关系，但系统匹配到实体后触发了直接因果推理分支，返回了其他无关因果。说明负面检查问题需要更明确的意图识别。

---

## 4. 大模型增强方法

### 4.1 系统架构

本项目采用 **GraphRAG（Graph Retrieval-Augmented Generation）** 架构，通过知识图谱检索增强大模型问答：

```
用户问题
    ↓
[实体匹配] → 识别问题中的医学实体
    ↓
[图谱检索] → 从 KG 中检索相关三元组、因果路径、条件证据
    ↓
[证据组装] → 将结构化证据格式化为自然语言提示词
    ↓
[LLM 生成] → Qwen API 基于图谱证据生成答案
    ↓
输出：答案 + 证据链 + 图谱不足说明
```

### 4.2 GraphRAG 检索模块

检索模块负责从知识图谱中获取与问题相关的结构化证据：

1. **实体匹配**：使用子串匹配识别问题中的实体。
2. **三元组检索**：对每个匹配实体，检索其所有相关三元组。
3. **多跳路径发现**：当问题包含多个实体时，搜索实体间的因果路径。
4. **条件证据检索**：查找相关因果关系的 condition_of 条件。
5. **证据去重与截断**：去重后限制返回证据数量。

### 4.3 三种问答模式

| 模式 | 说明 | 是否需要 API |
|---|---|---|
| `kg_only` | 仅使用知识图谱推理结果，模板式回答 | 否 |
| `llm_only` | 直接调用 Qwen API，不使用知识图谱 | 是 |
| `kg_augmented` | 先检索图谱证据，注入 Prompt，由 Qwen 生成答案 | 是（无 API 时退化为 kg_only） |

### 4.4 Prompt 设计

**KG-enhanced Prompt**（`prompts/kg_augmented_prompt.txt`）：

```
你是医学知识图谱问答助手。请严格基于给定的知识图谱证据回答问题。

要求：
1. 只能使用"知识图谱证据"中的事实。
2. 如果证据不足，请回答"知识图谱中没有足够证据"。
3. 必须输出证据链。
4. 不要编造图谱中没有的医学因果关系。
5. 不要给出诊断结论、药物剂量或处方建议。
6. 本系统仅用于医学知识学习和知识图谱课程实验，不构成医疗建议。

用户问题：{question}
知识图谱证据：{evidence}

请按以下格式回答：
答案：
证据链：
图谱不足或注意事项：
```

该 Prompt 的关键设计：
- **严格约束**：要求模型仅使用图谱证据，不编造知识。
- **证据可追溯**：强制输出证据链。
- **安全声明**：包含医疗免责声明。
- **不足说明**：要求模型指出图谱证据不足之处。

### 4.5 对比实验设计

实验在 50 个测试问题上同时运行三种问答模式，覆盖全部 7 种关系类型和 10 种推理类型。从以下角度进行对比分析：

| 评价维度 | 说明 |
|---|---|
| 准确性 | 回答是否与图谱事实一致 |
| 证据可追溯性 | 是否提供可验证的图谱证据链 |
| 幻觉减少 | 是否编造不存在的医学因果关系 |
| 多跳推理能力 | 能否发现正确的多跳因果路径 |
| 回答丰富度 | 回答的信息量和可读性 |

详见第 5 章实验结果。

---

## 5. 实验与结果分析

### 5.1 实验设置

#### 5.1.1 测试问题

系统从知识图谱中自动生成 **50 个测试问题**，覆盖全部 7 种关系类型和 11 种推理类型：

| 问题类型 | 数量 | 涉及关系 | 示例 |
|---|---|---|---|
| 直接因果推理（direct_causal） | 5 | `causes` | "急性扁桃体炎可能导致什么？" |
| 反向因果推理（reverse_causal） | 5 | `causes` | "影响发音可能由什么引起？" |
| 多跳因果链推理（multi_hop_causal） | 5 | `causes` | "为什么烟雾病可能和上位脑神经元受到压迫有关？" |
| 条件约束推理（condition_reasoning） | 5 | `condition_of` | "在什么条件下支气管哮喘可能发生？" |
| 上下位推理（is_a） | 5 | `is_a` | "阴道出血属于什么类型？" |
| 症状查询（symptom_query） | 5 | `symptom_of` | "双硫仑反应有哪些症状？" |
| 治疗方式查询（treatment_query） | 5 | `treated_by` | "血钙的异常如何治疗？" |
| 部位查询（location_query） | 5 | `located_in` | "脑出血发生在什么部位？" |
| 诊断方式查询（diagnosis_query） | 5 | `diagnosed_by` | "胃食管反流如何诊断？" |
| 负面检查（negative_check） | 3 | `causes` | "图谱中有没有证据说明A会导致B？" |
| 跨关系推理（cross_relation） | 2 | `symptom_of` + `causes` | "肝癌晚期有哪些症状？可能由什么引起？" |

#### 5.1.2 评价指标

| 指标 | 说明 |
|---|---|
| 图谱规模统计 | 实体数、三元组数、关系类型数 |
| 推理命中率 | 能否为测试问题返回有效证据 |
| LLM QA 对比 | 三种模式的准确性、证据可追溯性、幻觉率 |

### 5.2 图谱构建结果

详见 [2.5 图谱规模与质量](#25-图谱规模与质量)。

### 5.3 推理评估结果

在 50 个测试问题上运行知识推理模块，结果如下：

| 指标 | 值 |
|---|---|
| 测试问题数 | **50** |
| 整体推理命中率 | **94.0%**（47/50） |

分类型命中率：

| 推理类型 | 命中率 | 说明 |
|---|---|---|
| 直接因果推理 | 5/5 = **100%** | 全部命中 |
| 反向因果推理 | 5/5 = **100%** | 全部命中 |
| 多跳路径推理 | 5/5 = **100%** | 全部成功发现因果路径 |
| 条件约束推理 | 5/5 = **100%** | 全部命中 |
| 上下位推理 | 5/5 = **100%** | 全部命中 |
| 症状查询 | 4/5 = **80%** | 1 例因实体误匹配失败 |
| 治疗方式查询 | 5/5 = **100%** | 全部命中 |
| 部位查询 | 5/5 = **100%** | 全部命中 |
| 诊断方式查询 | 5/5 = **100%** | 全部命中 |
| 负面检查 | 1/3 = **33%** | 部分负面问题误触发因果推理 |
| 跨关系推理 | 2/2 = **100%** | 全部命中 |

因果推理类问题（直接/反向/多跳/条件/上下位）命中率为 **100%**。Qwen 扩展关系（`symptom_of`、`treated_by`、`located_in`、`diagnosed_by`）查询命中率整体为 **95%**。

### 5.4 大模型增强对比实验

在全部 50 个测试问题上分别运行三种问答模式（KG-only、LLM-only、KG-augmented），对比分析如下：

#### 5.4.1 对比案例

**案例 1：直接因果推理**

```
问题：急性扁桃体炎可能导致什么？

【KG-only】
  "急性扁桃体炎"可能导致：喉咙痛、咽口水疼痛、慢性扁桃体炎、急性肾炎、急性喉炎
  证据链：急性扁桃体炎 --causes--> 喉咙痛 等 8 条

【LLM-only】
  急性扁桃体炎可能引发的并发症包括：扁桃体周围脓肿、急性中耳炎、急性鼻炎、
  急性喉炎、急性支气管炎、风湿热、急性肾炎、心肌炎...
  （回答丰富但无法提供原文证据）

【KG-augmented】
  基于图谱证据回答，列出因果下游实体，并附原文证据链和图谱不足说明
```

**分析**：KG-only 提供了精确的图谱证据，但信息量有限；LLM-only 回答丰富但无法验证来源；KG-augmented 兼顾了准确性和可追溯性。

**案例 2：多跳因果链推理**

```
问题：为什么烟雾病可能和上位脑神经元受到压迫有关？

【KG-only】
  存在因果路径：烟雾病 --causes--> 脑出血 --causes--> 上位脑神经元受到压迫

【LLM-only】
  在主流医学理论中，烟雾病与上位脑神经元受压的关系并未被广泛确立...
  （倾向于否定该关系，但实际上图谱中存在该路径）

【KG-augmented】
  基于图谱证据，说明烟雾病可通过脑出血间接关联上位脑神经元受压
```

**分析**：对于多跳推理问题，LLM-only 可能因缺乏因果链条知识而否定正确的关系，KG-augmented 通过图谱证据提供可靠的多跳推理路径。

**案例 3：负面检查**

```
问题：图谱中有没有证据说明大便像果冻会导致慢性咽喉炎反复急性发作？

【KG-only】
  返回了无关的因果信息（误触发直接因果推理）

【LLM-only】
  回答了该关系在医学上的可能性，给出了较为通用的解释

【KG-augmented】
  基于图谱证据判断，说明图谱中没有直接证据支持该因果关系
```

**分析**：负面检查问题对三种模式均有挑战。KG-only 需要更好的意图识别来区分"是否存在"和"导致什么"；LLM-only 倾向于给出正面回答而非否定；KG-augmented 能够基于证据进行判断。

#### 5.4.2 三种模式对比总结

| 维度 | KG-only | LLM-only | KG-augmented |
|---|---|---|---|
| 准确性 | 高（基于图谱事实） | 中（可能包含错误） | 高（基于图谱证据约束） |
| 证据可追溯 | 是（提供三元组证据链） | 否（无法提供来源） | 是（提供图谱证据链） |
| 幻觉问题 | 无（严格基于图谱） | 存在（可能编造关系） | 低（约束模型使用图谱证据） |
| 回答丰富度 | 低（模板式回答） | 高（自然语言流畅） | 中高（基于证据的自然语言） |
| 多跳推理 | 支持（BFS 路径发现） | 不稳定（缺乏因果链知识） | 支持（检索路径后生成解释） |
| 需要 API | 否 | 是 | 是（可退化为 KG-only） |

### 5.5 局限性讨论

1. **实体匹配局限性**：子串匹配可能误匹配（如"症状"被当作实体）或漏匹配，需引入医学 NER 模型提升精度。
2. **负面问题处理**：系统缺乏明确的"是否存在"意图识别，负面检查问题容易误触发正向推理。
3. **评估方法**：当前评估使用自动生成的问题和模板匹配，缺少人工评估环节。
4. **LLM 回答质量**：KG-augmented 模式的回答质量受限于检索到的证据质量和 LLM 的指令遵循能力。

---

## 6. 可视化系统

### 6.1 图谱可视化

基于 pyvis 构建交互式图谱可视化（`results/kg_visualization.html`），展示知识图谱中的核心实体和关系。

**设计要点**：
- 展示图谱中覆盖全部 11 种实体类型的 568 个核心实体及其之间的 1,435 条关系（7 种关系类型全覆盖）
- 按实体类型着色（Disease 红色、Symptom 绿色、Treatment 蓝色等）
- 按关系类型着色（causes 红色、is_a 蓝色、symptom_of 绿色等）
- 节点大小按度值缩放，度值越高的实体节点越大
- 支持物理引擎拖拽交互，可缩放、平移、点击查看详情

**关系类型颜色映射**：

| 关系类型 | 颜色 |
|---|---|
| `causes` | 红色 |
| `risk_factor_for` | 橙色 |
| `condition_of` | 紫色 |
| `is_a` | 蓝色 |
| `symptom_of` | 绿色 |
| `treated_by` | 青色 |
| `located_in` | 黄色 |
| `diagnosed_by` | 深灰色 |

### 6.2 可视化问答系统

系统提供了基于 Gradio 的交互式问答界面（`src/qa_server.py`），整合了图谱可视化与问答功能：

- **问题输入**：支持自由输入自然语言问题，并提供 9 个预设示例问题，覆盖因果推理、症状查询、治疗查询、部位查询、诊断查询等多种推理类型。
- **三种问答模式切换**：KG-only / KG-augmented / LLM-only 三种模式可通过单选按钮切换。
- **证据检索**：独立按钮触发图谱证据检索，展示与问题相关的三元组、因果路径和条件证据。
- **图谱可视化集成**：右侧内嵌交互式图谱可视化，可在问答的同时浏览知识图谱结构。
- **图谱统计展示**：展示实体数量、三元组数量、关系类型、来源分布等核心指标。

启动方式：

```bash
conda activate chipkg
pip install gradio  # 首次运行需安装
python src/qa_server.py
# 浏览器访问 http://localhost:7860
```

---

## 7. 小组分工

<!-- TODO: 根据实际分工填写 -->

| 成员 | 任务 |
|---|---|
| 待填写 | 数据解析、Gold Seed KG 构建、Schema 设计、报告撰写 |
| 待填写 | Qwen 抽取 Prompt 设计、API 调用、抽取结果清洗 |
| 待填写 | 推理模块、GraphRAG 检索模块、大模型增强问答、评估实验 |

---

## 8. 总结与不足

### 8.1 项目总结

本项目完成了一个完整的医疗因果事件知识图谱构建与 GraphRAG 增强问答系统：

1. **图谱构建**：采用"Gold Seed + Qwen 抽取"混合策略，构建了包含 14,596 个实体、18,539 条三元组的大规模医疗因果知识图谱，覆盖 11 种实体类型和 7 种关系类型。
2. **Schema 设计**：设计了贴合医疗因果场景的本体，特别是 `condition_of` 关系建模了条件约束语义。
3. **信息抽取**：使用 Qwen LLM 从 1000 篇无标注文本中抽取了 9,413 条三元组，扩展了图谱覆盖范围。
4. **推理能力**：实现了 10 种推理类型，覆盖全部 7 种关系类型，在 50 个测试问题上整体命中率达 94%。
5. **大模型增强**：实现了 GraphRAG 问答系统，对比了 KG-only、LLM-only、KG-augmented 三种模式，验证了知识图谱在减少幻觉、提供证据链方面的增强效果。
6. **可视化**：基于 pyvis 构建了交互式图谱可视化。

### 8.2 不足与改进方向

1. **实体匹配**：当前使用子串匹配，未来可引入医学 NER 模型提升匹配精度。
2. **多跳推理**：BFS 可能发现语义偏移的路径，需加入语义相关性过滤。
3. **可视化问答**：当前仅支持命令行问答，可开发交互式 Web 界面。
4. **大模型增强评估**：需要更系统的人工评估和更细粒度的指标分析。

---

## 附录

### A. 项目代码结构

```
project/
├── README.md
├── requirements.txt
├── .env.example
├── data/
│   ├── raw/
│   │   ├── train_0717.json
│   │   ├── unlabel.json
│   │   ├── testA.json
│   │   ├── testB.json
│   │   └── example_code.txt
│   ├── processed/
│   │   ├── entities.csv
│   │   ├── relations.csv
│   │   ├── causal_events.csv
│   │   ├── kg.json
│   │   ├── qwen_extracted_raw.json
│   │   ├── qwen_extracted_clean.json
│   │   └── test_questions.json
│   └── triples.csv
├── src/
│   ├── config.py
│   ├── build_kg.py
│   ├── reasoning.py
│   ├── graph_retrieval.py
│   ├── llm_qa.py
│   ├── evaluate.py
│   └── relation_discovery.py
├── prompts/
│   ├── qwen_extraction_prompt.txt
│   ├── kg_augmented_prompt.txt
│   └── relation_discovery_prompt.txt
├── results/
│   ├── metrics.json
│   ├── extraction_metrics.json
│   ├── cases.md
│   └── kg_visualization.html
├── report.md
└── CLAUDE.md
```

### B. 环境配置

```bash
conda create -n chipkg python=3.10
conda activate chipkg
pip install -r requirements.txt
cp .env.example .env  # 编辑 .env 填写 QWEN_API_KEY
```

### C. 运行命令

```bash
# 构建图谱
python src/build_kg.py                                          # Gold Seed only
python src/build_kg.py --extract_qwen --max_qwen_docs 5         # 小规模测试
python src/build_kg.py --extract_qwen --qwen_files unlabel.json # 完整抽取

# 推理
python src/reasoning.py --query "高血压是否可能间接导致心肌梗死？"

# GraphRAG 检索
python src/graph_retrieval.py --question "为什么高血压可能和心肌梗死有关？"

# 大模型增强问答
python src/llm_qa.py --question "为什么高血压可能和心肌梗死有关？" --mode kg_only
python src/llm_qa.py --question "为什么高血压可能和心肌梗死有关？" --mode kg_augmented
python src/llm_qa.py --question "为什么高血压可能和心肌梗死有关？" --mode llm_only

# 评估
python src/evaluate.py
```
